---

---

<section id="inicio"
         class="relative min-h-screen flex items-center justify-center bg-gradient-to-br from-neutral-100 to-neutral-200 overflow-hidden pt-20">
    <!-- Background Image Overlay -->
    <div class="absolute inset-0 bg-black bg-opacity-50 ">
        <img src="/background-inicio.jpg" alt="Hero Background"
             class="w-full h-full object-cover object-center  absolute inset-0 brightness-50">
    </div>

    <!-- Background decorative elements -->
    <div class="absolute inset-0 overflow-hidden">
        <div class="absolute top-20 right-10 w-32 h-32 bg-primary-400 rounded-full opacity-20 animate-float"></div>
        <div class="absolute bottom-20 left-10 w-24 h-24 bg-secondary-400 rounded-full opacity-20 animate-float"
             style="animation-delay: 1s;"></div>
    </div>
    <!-- Content -->
    <div class="relative z-10 container mx-auto px-4 lg:px-8">
        <div class="max-w-4xl">
            <!-- Main Heading -->
            <div class="animate-fade-in-up">
                <p class="text-white/90 text-lg md:text-xl font-medium mb-4 tracking-wide drop-shadow-2xl hover:-translate-y-2 hover:scale-105 transition-all duration-500 cursor-pointer"
                   style="text-shadow: 2px 2px 4px rgba(0,0,0,0.8), 0 0 8px rgba(0,0,0,0.5);">
                    ENTREGAMOS RÁPIDO, SEGURO Y
                </p>
                <h1 class="text-white font-display drop-shadow-2xl font-black text-5xl md:text-7xl lg:text-8xl leading-none mb-8 transform -skew-x-6 hover:-translate-y-4 hover:scale-105 transition-all duration-700 cursor-pointer"
                    style="text-shadow: 3px 3px 0px rgba(0,0,0,0.8), 6px 6px 0px rgba(0,0,0,0.6), 9px 9px 0px rgba(0,0,0,0.4), 0 0 20px rgba(0,0,0,0.7);">
                    SIN COMPLICACIONES
                </h1>
            </div>

            <!-- Description -->
            <div class="animate-slide-in-left animation-delay-300">
                <p class="text-white/90 text-lg md:text-xl max-w-md mb-8 leading-relaxed"
                   style="text-shadow: 2px 2px 4px rgba(0,0,0,0.8), 0 0 8px rgba(0,0,0,0.5);">

                    Envíos Same Day y Next Day en Lima y Callao desde 6 soles, con seguimiento en tiempo real
                </p>
            </div>

            <!-- CTA Buttons -->
            <div class="flex flex-col sm:flex-row gap-4 animate-slide-in-left animation-delay-500">
                <button class="bg-primary-50 hover:bg-primary-600 text-white font-semibold px-8 py-4 rounded-xl transition-all duration-300 transform hover:scale-105 hover:shadow-strong">
                    Cotiza ahora
                </button>
                <button class="border-2 border-white/30 hover:border-white text-white font-semibold px-8 py-4 rounded-xl transition-all duration-300 backdrop-blur-sm hover:backdrop-blur-md hover:bg-white/10">
                    Ver servicios
                </button>
            </div>
        </div>
    </div>
    <!-- Trust Indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 z-10 animate-fade-in animation-delay-700">
        <p class="text-white/80 text-center text-sm md:text-base font-medium">
            Más de 50 empresas confían en nosotros cada día.
        </p>
    </div>
</section>