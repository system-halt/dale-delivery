---
import ContactForm from '../components/ui/ContactForm.tsx';
---

<section id="contacto" class="relative py-12 sm:py-16 lg:py-20 overflow-hidden">
    <div class="container mx-auto px-4 relative z-10">
        <!-- grid con altura completa -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-stretch">

            <!-- Content Section -->
            <div class="animate-on-scroll relative flex flex-col h-full">
                <!-- Background Image SOLO aquí -->
                <div class="absolute inset-0 z-0">
                    <img
                            src="rayo.png"
                            alt="Background"
                            class="w-full h-full object-cover"
                    />
                    <div class="absolute inset-0 bg-white/90"></div>
                </div>

                <div class="relative z-10 flex flex-col h-full">
                    <div class="mb-6 sm:mb-8">
                        <h2 class="font-display font-bold text-2xl sm:text-3xl lg:text-4xl text-neutral-900 mb-3 sm:mb-4 leading-tight">
                            Empieza a potenciar tus envíos con
                            <span class="text-orange-500 font-black">DALE DELIVERY</span>
                        </h2>
                        <p class="text-neutral-700 text-base sm:text-lg leading-relaxed">
                            Compártenos tus datos y un especialista te brindará
                            <span class="font-semibold text-orange-600">asesoría personalizada</span>,
                            respondiendo todas tus dudas y mostrándote en tiempo real cómo nuestras
                            <span class="font-semibold">soluciones logísticas</span>, seguras, eficientes y a la medida pueden
                            hacer crecer tu negocio.
                        </p>
                    </div>

                    <div class="bg-gradient-to-r from-blue-500 to-blue-600 rounded-xl sm:rounded-2xl p-4 sm:p-6 text-white mb-6 sm:mb-8 mt-auto">
                        <p class="text-base sm:text-lg font-medium mb-2">
                            En <span class="font-bold italic">Dale Delivery</span>, cada entrega es más que un paquete:
                            es la <span class="font-bold text-blue-100">conexión entre emprendedores y clientes</span>.
                        </p>
                        <p class="text-blue-100 text-sm sm:text-base">
                            Somos tu aliado para que tu esfuerzo llegue a <span class="font-semibold">todo el país</span> y
                            marque la <span class="font-semibold">diferencia</span> en cada envío.
                        </p>
                    </div>
                </div>
            </div>

            <!-- Contact Form -->
            <div class="animate-on-scroll flex h-full" style="animation-delay: 0.2s;">
                <div class="bg-white rounded-xl sm:rounded-2xl p-6 sm:p-8 shadow-2xl border border-gray-100 flex flex-col w-full h-full">
                    <ContactForm client:load/>
                </div>
            </div>
        </div>
    </div>

</section>

<style>
    .contact-form {
        display: flex;
        flex-direction: column;
        gap: 0;
    }

    .submit-btn {
        width: 100%;
        background: linear-gradient(135deg, #3b82f6, #1d4ed8);
        color: white;
        font-weight: 600;
        padding: 1rem 1.5rem;
        border-radius: 0.75rem;
        border: none;
        font-size: 0.875rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        transition: all 0.3s ease;
        cursor: pointer;
        box-shadow: 0 4px 14px rgba(59, 130, 246, 0.3);
        margin-bottom: 1rem;
    }

    .submit-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(59, 130, 246, 0.4);
        background: linear-gradient(135deg, #2563eb, #1e40af);
    }

    .submit-btn:active {
        transform: translateY(0);
    }

    .privacy-notice {
        font-size: 0.75rem;
        color: #6b7280;
        text-align: center;
        line-height: 1.4;
        margin: 0;
    }

    .animate-on-scroll {
        opacity: 1;
        transform: translateY(0);
    }

    @media (min-width: 640px) {
        .submit-btn {
            padding: 1.25rem 2rem;
            border-radius: 1rem;
            font-size: 1rem;
        }
    }
</style>